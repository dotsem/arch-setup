- set usb autosuspend to 0
- separate essentials & non-essential packages
- sysunit (system unittest)
  - check if everything works in the system
  - should be a command that can be run from the terminal
- apres setup script
  - a script to setup non-essential packages, test the system (with sysunit)
  - progress should be stored in a file, so you can do it in intervals
  - should run in the background
  - should be a command that can be run from the terminal (disappear when everything is set up)
- update packages to more efficient versions (bin versions)
- add extra setup
- add setup for my taskbar (https://github.com/dotsem/kolibri-shell)
  - uses flutter, should be built before able to run, this means flutter environment should be setup